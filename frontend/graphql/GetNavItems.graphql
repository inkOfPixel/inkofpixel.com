query GetNavItems($locale: String) {
  navigations(locale: $locale) {
    id
    locale
    items {
      ... on ComponentMenuLink {
        id
        linkName
        __typename
        path {
          path
          pageName
        }
      }
    }
  }
}
