query getProjectsList {
  projectsList(id: 1) {
    id
    locale
    projects {
      id
      path
      companyName
      projectType
      description
      linkName
      linkPath
      image {
        id
        url
        alternativeText
      }
    }
  }
}
