mutation saveChanges(
  $pageInput: updatePageInput
  $globalInput: updateGlobalInput
  $topbarInput: updateMenuInput
) {
  updatePage(input: $pageInput) {
    page {
      id
    }
  }

  updateGlobal(input: $globalInput) {
    global {
      companyData {
        id
      }
      footer {
        id
      }
    }
  }

  updateMenu(input: $topbarInput) {
    menu {
      id
      title
      links {
        id
        url
        label
      }
    }
  }
}
